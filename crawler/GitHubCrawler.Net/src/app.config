<?xml version="1.0" encoding="utf-8"?>
<configuration>
	<appSettings>
		<!-- if set, limits the crawling to these repos only -->
    	<add key="cv_repo" value="alexro/cv" />
    	<add key="jobs_repo" value="gitspirit/jobs" />
  	</appSettings>
</configuration>
